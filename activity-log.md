# Activity Log

Structured changelog of completed epics, most recent first. Populated by the
`/log` command (see `.claude/skills/aidd-log/SKILL.md`) — only completed,
user-facing epics belong here, not routine commits.

## Format

```
## $date

- $emoji - $epicName - $briefDescription
```
